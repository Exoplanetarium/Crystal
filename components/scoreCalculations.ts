// export const calculateGoalsScore = (goalsArray: { status: string }[]) => {
//     const completedGoals = goalsArray.filter(goal => goal.status === 'completed').length;
//     const inProgressGoals = goalsArray.filter(goal => goal.status === 'in progress').length;
//     const notStartedGoals = goalsArray.filter(goal => goal.status === 'not started').length;

//     const goalsScore = (completedGoals * 1 + inProgressGoals * 0.5 + notStartedGoals * 0)
//         / (completedGoals + inProgressGoals + notStartedGoals) * 100;

//     return goalsScore;
// };

// export const calculateEnvironmentScore = (envArray: {}[]) => {

// };

// export const calculateCertificationsScore = (certArray: {}[]) => {

// };

// export const calculateTransparencyScore = (transparencyArray: {}[]) => {

// };
